name: 'Autorelease'
description: 'Release every commit on `release/**` branches'
inputs:
  GITHUB_TOKEN:  # id of input
    description: 'GitHub Auth token'
    required: true
  APPLICATIONS:  # id of input
    description: 'JSON array of projects to autorelease, eg: [{"name":"web-admin"}, {"name":"web-video"}]'
    required: true
  RELEASE_TAG:  # id of input
    description: 'Pass in a release tag to trigger a non rc release'
    required: false
runs:
  using: 'node12'
  main: './dist/index.js'